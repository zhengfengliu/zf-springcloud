eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8880/eureka/
spring:
  rabbitmq:
    host: localhost
    port: 5672
hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: false 
management:
  security:
    enabled: false
server:
  port:10081
